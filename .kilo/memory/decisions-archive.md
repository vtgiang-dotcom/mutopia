---
type: project
created: 2026-08-09
---

# Decisions Archive

> Cold storage for decisions pruned out of `MEMORY.md` to stay under the
> `memory_gate` hard cap (8,000 chars). NOT loaded automatically into any
> session — unlike `MEMORY.md`, this file has no size limit and no
> auto-injection. Grep this file on demand for the "why" behind an old
> decision that `git log` alone makes hard to find.
>
> Workflow: when `MEMORY.md`'s Decisions section approaches the cap, MOVE
> (don't delete) the oldest/least-referenced entry here verbatim, then keep
> pruning until back under the WARN threshold (4,000 chars).

## Decisions

- (moved-out entries land here as this project's memory grows)
# Memory Index

> Persistent cross-session memory for THIS project. The AI reads this at
> session start and writes to it via `/remember`. Starts blank — Solo-Code-
> CLI's own accumulated memory is never deployed into target projects.

## Project
- [project] (add your branch naming / workflow conventions here)

## Rules
- [rules] Load AGENTS.md for all behavior rules -> [[AGENTS.md]]

## Decisions
- **Client Data Baseline Sync (2026-08-09)**: Synchronized 100% of Season 6E3 client Data assets (975 files including `Sound/`, `Music/`, `Local/`, `Macro.txt`, map objects) from `MU Client 1.04d - Season 6E3` into `MuMain-main/src/bin/Data` and release output `build/src/Release/Data`.
- **Client Native AOT & UI Fix (2026-08-09)**: Target Framework of `ClientLibrary.csproj` aligned to `net9.0` for OpenMU compatibility; patched character creation UI 2D projection reset (`BeginBitmap()`) in `CharMakeWin.cpp`.
- **S20 Class Shift Fix (2026-08-11)**: Removed 3-bit left shift on the server's `CharacterCreationSuccessful` packet (`ServerToClientPackets.xml`, `.cs`, `.csRef`) and corresponding right shift on the client side (`WSclient.cpp:L761`), fixing the truncation of S20 class numbers (indices >= 32 like Slayer, Lemuria, Rune Wizard) and preventing them from loading/saving as incorrect S6 classes (like Dark Lord).
- **Docker/Client Build Sync (2026-08-11)**: Rebuilt the server startup image `openmu-startup` and updated the client binary `src/bin/Main.exe` with the class shift fix.
- **⚠️ CRITICAL: RUN THE GAME FROM `build/src/Release`, NOT `src/bin` (2026-08-13)**. There are TWO `Main.exe` in the repo and they get out of sync. The authoritative one is `MuMain-main/build/src/Release/Main.exe` (produced by MSBuild, with all fixes). `MuMain-main/src/bin/Main.exe` is a stale copy (last touched 2026-08-11) and does NOT contain any S20 fixes. Always rebuild+run from `build/src/Release`. The build pipeline copies assets from `src/bin` → `build/src/Release` automatically on every Main.vcxproj build.
- **S20 Character Creation Root Cause (2026-08-13)**: Character creation saved wrong classes (e.g. Alchemist→RuneWizard=32, Illusion→DarkLord=16). Root cause: `CreateCharacter` packet's `Class` field is defined with `<LeftShifted>2</LeftShifted>` + `<Length>6</Length>`, so the generated setter `CreateCharacterRef.Class` ALREADY does `<< 2` + 6-bit mask internally. The old client code `(CharacterView.Class << 2)` shifted TWICE. Fix: added `CCharacterManager::ClientClassTypeToServerClassNumber(CLASS_TYPE)` (returns raw class number 0..56 as `BYTE`, in `CharacterManager.h/.cpp`) and `CharMakeWin.cpp:429` now calls it and casts to `CharacterClassNumber`. Do NOT re-add any manual `<< 2` in the create path.
- **Server AppearanceSerializer Version Gates (2026-08-13)**: Fixed plugin selection so the raw-byte Extended serializer is used for S6+ clients. `AppearanceSerializerExtended.cs` `MinimumClient(106,3)` → `(6,3)`; `AppearanceSerializer.cs` added `MaximumClient(5,0)`; `AppearanceSerializer095.cs` added `MaximumClient(0,97)`; `AppearanceSerializer075.cs` added `MaximumClient(0,89)`; `ShowCharacterListExtendedPlugIn.cs` `MinimumClient(106,3)` → `(6,3)`. Client version `20404` resolves to `(106,3)`, `10404` to `(6,3)`.
- **Author's `update/` is S6E3 vanilla (2026-08-13)**: `D:\Project\mu\update\{MuMain-main,OpenMU-master}` is the author's original S6E3 codebase — NO S20 support (MAX_CLASS=7, CharacterClassNumber ends at FistMaster=25). All S20 support is custom-built. The REAL S20 client is `[MUDevs Emulator] Base Client S20.2-3` and `[MUDevs Emulator - FREE] FULL Base Season 20.2-3`.
- **S20 Data completeness (2026-08-13)**: `NewFace08-15.bmd` are REAL S20 models (identical byte-size to `[MUDevs Emulator] Base Client S20.2-3\Data\Logo`). Player/ = 1144 files (S20: 1143) ✅; Skill/ = 633 (S20: 633) ✅; Monster/ = 2145 (S20: 2157) ~✅; **Item/ = 3747 vs S20 8426 — MISSING ~4700 item files** (weapons/equipment). To reach FULL S20, copy `Item/` from the S20 client into `src/bin/Data/Item`.
- **Docker image optimization (2026-08-13)**: Server image reduced 388MB → 374MB by stripping `*.pdb` and `Microsoft.CodeAnalysis*.dll` in `src/Startup/Dockerfile`. DO NOT delete `*.xml` or `*.staticwebassets.*.json` — those are required at runtime (ConnectionSettings.xml is config, staticwebassets manifest is needed by AdminPanel).
- **S20 FULLY REVERTED → S6E3 (2026-08-13) — DECISION**: Abandoned Season 20 (and S9/S13-14) and reverted the codebase back to pure Season 6E3. Reason: the S6E3 client's BMD loader (`ZzzBMD.cpp`) only supports model versions 0x0A/0x0C/0x0E; S20 uses BMD version 0x0F (and S9's newer files use 0x0E) with a different/unknown encryption key — the `webzen#@!01...` comment key does NOT decrypt v14 (tested 4 variants, all failed). Result: 76% of S20 Item models, ~36% Monster, 100% Object1 maps would be invisible. **Max practical season is S6E3** unless a community-sourced v14/v15 codec is found. S20 data source folders (`[MUDevs Emulator] Base Client S20.2-3`, `[MUDevs Emulator - FREE] FULL Base Season 20.2-3`) and `muss9` may still exist on disk.
- **S20 revert scope (2026-08-13) — what was cleaned**: (1) Client: 12 files reverted from `update/MuMain-main` (`_enum.h`, `_define.h`, `CharacterManager.cpp/h`, `CharMakeWin.cpp/h`, `PacketFunctions_CommonEnums.h`, `WSclient.h/cpp`, `ZzzCharacter.cpp`, `ZzzObject.cpp`, `ZzzOpenData.cpp`). (2) Server: deleted 22 files (8 `Class*.cs` + 11 `AddLanceFor*/AddMagicalShotFor*/AddClash*/AddCrescentMoonSlashFor*`), reverted `CharacterClassNumber.cs`, `CharacterClasses.cs`, `CharacterClassHelper.cs`, `CharacterClassInitialization.cs`, `CommonEnums.xml/cs`, `SkillNumber.cs`, `MasterSkillTree.xml`, 4 serializers + `ShowCharacterListExtendedPlugIn.cs`, and `SkillsInitializer.cs` (CrescentMoonSlash→AllKnights, Lance→SoulMasterAndGrandMaster|AllSummoners, removed MagicalShot+Clash). (3) DB: deleted 53 S20 characters, 8 `AccountCharacterClass` rows, 23 `config.CharacterClass` (number>=28); only 18 S6 classes (0-25) remain.
- **⚠️ PRESERVE — user's BOT AI + legit S6 features (do NOT treat as S20 garbage)**: `GameLogic/Bots/*` (16 files), `GameLogic/PlugIns/BotAI/*`, `ChatCommands/AddBotsChatCommandPlugIn.cs`, `VersionSeasonSix/RuudMerchantStore.cs`, `VersionSeasonSix/Maps/*` (16 maps: Acheron, Deventer, Urk Mountain, Nars, Ferea, Nixies Lake, DeepDungeon, Swamp of Darkness/Doom, Kubera Mine, Atlans Abyss), AdminPanel `BotDashboardController.cs`/`Bots.razor`/`BotLiveDto.cs`, Castle Siege migration, Resets system. `MoveCommandData.cpp` maps were relabeled S20→S6 (they are S6 maps).
- **Author `update/` diff = refactor only, NOT applied (2026-08-13) — DECISION**: The only files the author has that current lacks are (1) 9 `GameLogic/*Extensions.cs` split out of the 2621-line `Player.cs` monolith, and (2) 7 `Web/Shared/Components/Toast/*` files replacing the `Blazored.Toast` NuGet package with a self-written toast. Both are **refactors with zero functional benefit** — the same logic already lives inside current `Player.cs`, and `Blazored.Toast` v4.2.1 already works in AdminPanel. Decision: do NOT apply — splitting a 2600-line file offers no value for a solo developer not actively editing `Player.cs`, and swapping a working dependency introduces risk for no gain. Revisit only if `Player.cs` grows >~4000 lines, is edited frequently, or a second developer/AI works on it in parallel.
- **Player website REBUILT as OpenMU.PlayerWeb (2026-08-13) — DECISION**: Rewrote the player-facing website (old `open-mu-web-master` Next.js 14, 1.61GB image) as a Blazor Web App (.NET 9) in `OpenMU.PlayerWeb/`, replacing the Next.js `openmu-web` service in docker-compose. Stack: Static SSR + Interactive Server (forms only), Cookie auth (self-written), EF Core + Npgsql mapping directly into the OpenMU PostgreSQL schemas (config/data/guild), BCrypt.Net-Next (work factor 11 to match OpenMU `$2a$11$`). Design follows the `template/` "Blood & Gold Eternal" dark-fantasy system (Newsreader/Work Sans/JetBrains Mono, #8B0000 blood red + #FFD700 gold, sharp edges, btn-ornate/panel-metallic/rank-table). New image is 339MB (vs 1.61GB). Port 3007. Services: AccountService, CharacterService (reset/addstats/pkclear/resetStats — same stat/map UUIDs as the old Next.js), RankingService, NewsService (data.OpenMuWeb_News), GuildService, ServerStatusService (proxies GAMESERVER_URL/api/status), BotService. Pages: Home, Classes, Maps, Rankings (4 tabs), News, NewsDetail, Download, Info, Terms, Account (index + change password), Login, Register, Logout, Admin/News, Admin/Bots (GM role = CharacterStatus 32). Template images are Google placeholders (will expire — replace with real server art later). **`open-mu-web-master/` (old Next.js) has been DELETED.**
- **⚠️ Tailwind CDN required for PlayerWeb (2026-08-13)**: The Razor pages use Tailwind utility classes (flex/grid/gap/py-*/btn-ornate/etc). The site initially rendered as unstyled text because Tailwind was never loaded. `Components/App.razor` now loads `https://cdn.tailwindcss.com` + a `tailwind.config` with the full "Blood & Gold" color palette (surface/primary/secondary tokens). If layout ever breaks again, check that the CDN script + config are still in App.razor's <head>.
- **⚠️ CRITICAL: DO NOT delete Microsoft.CodeAnalysis*.dll from the server image (2026-08-13)**: OpenMU compiles its plugins at runtime with Roslyn (`Microsoft.CodeAnalysis.CSharp`). A previous image "optimization" stripped `Microsoft.CodeAnalysis*.dll` (thinking it was build-only), which broke bot generation and ALL dynamic plugins — the server logged `FileNotFoundException: Could not load file or assembly 'Microsoft.CodeAnalysis.CSharp, Version=4.14.0.0'` and bots created accounts but never spawned characters (51/100 bot accounts were char-less). Fixed by removing that `find -delete` line from `src/Startup/Dockerfile`. Only `*.pdb` is safe to strip. The image stays ~374MB with Roslyn included.
- **Bot name generator is now a CURATED LIST, not procedural (2026-08-13)**: `GameLogic/Bots/BotNameGenerator.cs` was rewritten to draw from a fixed 698-name pool (LOTR/GOT/Witcher/Star Wars/Star Trek/Warcraft/Dune/MU-themed) instead of procedural syllable concatenation. All names satisfy MU's `CharacterNameRegex` (`^[a-zA-Z0-9]{3,10}$`, set in `GameConfigurationInitializerBase.cs:55` + DB `config.GameConfiguration`). Names >10 chars were truncated (e.g. GrandMaster→GrandMaste). The regex limit of 10 is vanilla MU — do NOT raise it casually (client S6E3 truncates long names).
- **Phases 0-5 Upgrade Implementation & Verification (2026-08-13)**: Completed Phase 0 (clean stale src/bin, activate StatReset), Phase 1 & 2 (activate 8 GM ChatCommands, 3 Invasions, 3 MiniGame commands, DB migrations), Phase 5 & 3 Web Portal (`Rename.razor`, `CharacterService.RenameAsync`, `VipService`, `WheelService`, `LuckyWheel.razor`, `Admin/Vip.razor`, `Account/Index.razor`), and Phase 4 Client C++ (`DiscordRPC.h/cpp` via Win32 named pipe IPC, wired into `Winmain.cpp`, `MainScene.cpp`, `SceneManager.cpp`). Both `OpenMU.PlayerWeb` and `Main.exe` built cleanly with 0 errors. Verified 15/15 plugin GUIDs in PostgreSQL (`IsActive=true`). Rebuilt and deployed `mu-openmu-playerweb` Docker container to port 3007.
- **Blazor Cookie Authentication & News Table Fix (2026-08-13)**: Fixed `Sign in` failing on `OpenMU.PlayerWeb`. Root cause 1: `Login.razor` previously ran under `@rendermode InteractiveServer` over a WebSocket SignalR circuit where `HttpContext.SignInAsync` cannot write HTTP response cookies (headers already sent). Solution: Mapped standard HTTP POST `/api/auth/login` and GET `/logout` endpoints in `Program.cs` and converted `Login.razor` to a Static SSR form submitting to `/api/auth/login`. Root cause 2: `data."OpenMuWeb_News"` table was missing in PostgreSQL causing unhandled exception on Home/Redirect. Solution: Created table `data."OpenMuWeb_News"` and inserted default welcome post. Web portal now returns HTTP 200 OK across all pages.
- **Server-side VIP EXP Bonus Implementation (2026-08-13)**: Added `VipTier` and `VipExpiry` properties to `OpenMU-master/src/DataModel/Entities/Account.cs`. Added `GetVipExpMultiplier()` method to `Player.cs` (Silver=1.2x, Gold=1.5x, Platinum=2.0x when active). Applied `GetVipExpMultiplier()` inside `Player.cs` (`CalculateExpAfterKill`) and `Party.cs` (`DistributeExperienceToPartyMemberAsync`). Built `Startup.csproj` (0 errors) and redeployed `openmu-server` Docker container.
- **Bot Personality Dashboard Upgrade (2026-08-13)**: Upgraded AdminPanel Bot AI Dashboard (`http://localhost:8081/bots`). Resolved real personality archetypes (`BotPersonalityResolver.Resolve`) for live bots (`Balanced`, `Greedy`, `Warrior`, `Loner`, `Guardian`, `Reckless`). Added Personality Breakdown stats bar, Top Farmer metric, and Personality Filter dropdown to UI. Rebuilt `MUnique.OpenMU.Startup.csproj` (0 errors) and redeployed `openmu-server` Docker container.
- **Bot AI System Consolidation & Humanized Chat (2026-08-13)**: Audited dual bot systems (`GameLogic/Bots` vs `GameLogic/PlugIns/BotAI`). Verified `GameLogic/Bots` is active source of truth. Created `BotChatHandler.cs` (IChatMessageReceivedPlugIn) to give bots natural, intent-based chat responses without fake promises (integrated with `BotPartyHandler` for real party requests). Broadcasts chat overhead to all nearby map observers (`ForEachWorldObserverAsync<IChatViewPlugIn>`). Includes TTL-pruned rate limiting and proactive death reactions (`BotRevengePlugIn.cs`). Marked `SmartBotFeaturePlugIn.cs` as Obsolete. Added description metadata to `AddBotsChatCommandPlugIn.cs`. 100% of all 719 unit tests passed, 0 build errors. Report written to `.gemini/antigravity/handoff/outbox/bot-ai-consolidation-report.md`.
- **Client Codebase Audit & Data Asset Synchronization (2026-08-13)**: Audited `MuMain-main/src` against reference repo `update/MuMain-main/src`. Confirmed codebase is 100% clean Season 6 Episode 3 standard with ONLY `DiscordRPC.h/cpp` added. Resolved CMake VS2022 output path (`build/Release/Main.exe`). Performed full clean build of Client `Main.exe` (Timestamp: 8:25:33 PM) with watermark tag `=== S6E3 CLEAN CLIENT ===`. Synced clean binary across all execution paths (`build/Release`, `build/src/Release`, `src/Main.exe`, `src/bin/Main.exe`).
- **Part A Simulator Final Calibration Verified (2026-08-17)**: Replaced hardcoded monster selector with dynamic LINQ ranking based on unpenalized EXP/h (`ScoreMonster`), generated diagnostic `monster_coverage_report.csv` (correctly identifying dominated Coolutin #457 at L240), verified Zen Cap All-or-Nothing overflow unit test, added `ResetPaid` blocked flags to Scenarios A/C2/D2/F/GoldPreset, and regenerated all 15 CSV files with 100% acceptance gates passed (8/8 PASS). Source code audit frozen.
- **Simulator V3 Specification & Gate Audit Handoff Documented (2026-08-18)**: Saved complete Round 3 specifications (Gate level audit from `Gates.cs` [Raklion=280, KanturuRelics=230, etc.], per-kill Zen cap granularity, `ResetPaid` execution control, stale CSV cleanup, and 9 acceptance criteria) in `SIMULATOR_V3_SPECIFICATION_HANDOFF.md` to revisit after Season 16 evolution.
- **Season 16 Full Asset Ecosystem & Technical Specification Available (2026-08-18)**: Added complete Season 16 package in `sourecodeadd/` consisting of (1) `ZhyperMU S16 Full` (100% client assets, 162 subdirectories, 137 3D maps, items, monsters, sounds), (2) `MuClientTools16` (1,544 tool files & standalone native .exe decoders for all S16 BMD/ATT/OZ* formats), (3) `Server - LgdMu s16 1.1` & `Lgd-Server-main` (Full C++ GameServer/ConnectServer/LoginServer + MySQL DBs, complete packet maps in `ClientPacket.h`, mix formulas, elemental pentagram, Muun pet, Majestic skill tree), (4) `Client - LgdMu S16 1.1` (C++ `Main.dll` v1.1 hook with auto-reconnect, anti-cheat, custom objects), (5) `MuOnline-WorldEditor-master` (C++ ImGui realtime map editor). Decision: This provides both a standalone S16 environment and a ground-truth specification to optionally upgrade OpenMU C# or downport assets.
- **S16 Server Restored to Runbook State (2026-08-18) — DECISION**: Restored the S16 runtime after repo restructure. Three root causes fixed to get `GameServer.exe` booting: (1) **UTF-8 BOM** in the 5 root `.conf` files under `D:\Project\mutopia\server-s16\` broke `boost::property_tree::ini_parser` → section name parsed as `\xEF\xBB\xBF[SERVER]` so every key (incl. `GameServer.Database.Info`) read empty → "GameServer database not specified". Stripped BOM from all 5. (2) **`server_list` schema wrong** — Gemini's auto-generated table lacked column `display_id`, so ConnectServer's index-based `LoadServerList()` (reads `fields[0..10]`) shifted every column. Recreated 11-column table + inserted `flag=3`. (3) **missing `libmysql.dll` in `GameServer\`** — GameServer crashed mid-handshake; copying `libmysql.dll` from root into `GameServer\` fixed it. After these, GameServer reached `GameServer Ready` + all 3 internal handshakes (SERVERLINK 55555, AUTHSERVER 44417, CONNECTSERVER 44419).
- **S16 GameServer crash #2 — RESOLVED (2026-08-18)**: Root cause = **`monster_template` table empty (0 rows)** while `monster` spawn table had 10,220 rows referencing template ids. When GameServer spawned monsters from `monster`, `MonsterManager::LoadMonsterTemplate()` (`Game/MonsterManager.cpp:65`) found no template → null deref in a Boost.Asio worker thread → hard access violation (not caught by UpdateLoop's try/catch, hence no "FATAL EXCEPTION" line). The crash "moved" (19s→39s) as more data was loaded because more async work happened. **Fix = re-import `Databases\Game\monster - complete.sql` LAST** (it contains 786 `INSERT monster_template` + 10,220 `INSERT monster`). ⚠️ **Import-order trap**: `monster_template.sql` (3.3KB) is schema-only with `DROP TABLE` — importing it AFTER `monster - complete.sql` wipes the 786 templates. The 18 `[DIAG2]` log markers are still in `Game/Main.cpp` (harmless, leave them). After fix: `Loaded 786 monsters definitions`, tick passed 1405+, GameServer stable >2min, port 55902 listening. The "High Monster GUID" ERROR lines are benign (informational spawn-guid notices, not fatal).
- **S16 Part B data loaded (2026-08-18)**: Imported `monster - complete.sql` → `monster`=10,220, `monster_template`=786, `monster_skill`=342, `monster_skill_special`=160, `monster_ai_automata`=164, `monster_ai_unit`=47, `monster_ai_group`=88, `monster_respawn_location`=33, `event_manager`=362. Client runtime copied to `D:\Project\mutopia\`: `client-s16\` (30,584 files / 2.12 GB — Data + main.exe 25.5MB), `client-s6\` (19,952 files / 1.57 GB — Data + Main.exe), `launcher\` (MuLauncher.exe + servers.json). S16 client exe = `main.exe` (25.5MB) from `reference\s16-data\MuOnline_S16_Lgd-main\Main\`; S6 = `Main.exe` from `src\client-s6\src\bin\`.
- **⚠️ S6 client authoritative binary GONE (2026-08-18)**: restructure DELETED `MuMain-main/build/` (3.5 GB) which held the authoritative `build/src/Release/Main.exe`. Only `src/client-s6/src/bin/Main.exe` remains (stale 2026-08-11). Rebuild via CMake/MSBuild when a fresh client binary is needed.
- **S16 Server Restored to Runbook State (2026-08-18) — DECISION**: Restored the S16 runtime after repo restructure. Three root causes fixed to get `GameServer.exe` booting: (1) **UTF-8 BOM** in the 5 root `.conf` files under `D:\Project\mutopia\server-s16\` (`connectserver.conf`, `gameserver.conf`, `game_common.conf`, `loginserver.conf`, `serverlink.conf`) broke `boost::property_tree::ini_parser` — it parsed section name as `\xEF\xBB\xBF[SERVER]` so every key (incl. `GameServer.Database.Info`) read empty → "GameServer database not specified". Stripped BOM from all 5. (2) **`server_list` schema wrong** — Gemini's auto-generated table lacked column `display_id`, so ConnectServer's index-based `LoadServerList()` (reads `fields[0..10]` = code, display_id, name, port, ip, flag, online, default_world, default_x, default_y, type) shifted every column → log showed `Port: 127 IP: 1 Disabled: 1`. Recreated the 11-column table + inserted `flag=3` (DISPLAY|ACCOUNT_ALLOWED, no DISABLED). (3) **missing `libmysql.dll` in `GameServer\`** — GameServer crashed mid-handshake; copying `libmysql.dll` from root into `GameServer\` fixed it. After these, GameServer reached `GameServer Ready` + all 3 internal handshakes (SERVERLINK 55555, AUTHSERVER 44417, CONNECTSERVER 44419) succeeded. Remaining open issue: GameServer crashes at `Loading Castle Siege NPC From ServerLink` — likely a struct-layout/packet mismatch because the 4 binaries were built at different times (GameServer 14:21 vs others 13:41); fix = rebuild all 4 from one consistent build. **S16 stack: MySQL 5.6 Docker (port 3307) with 4 DBs (`database_login`=16 tbl, `database_game`=179, `database_characters`=97, `database_log`=21); runtime binaries + conf in `D:\Project\mutopia\server-s16\`; start order = LoginServer → ConnectServer → ServerLink → GameServer.**
- **Season 16 Multi-Realm Architecture & Client Strategy Assessment (2026-08-19) — DECISION**:
  - **Server S16 Native (`src/server-s16` - C++ MSVC Win32)**: Verified 100% compiled clean with 0 errors (`GameServer.exe`, `ConnectServer.exe`, `DataServer.exe`), schema validated on SQL Server 2022 (63 tables + Castle Siege, Character, Item data). Full Season 16 business logic (10 classes, Gun Crusher/Slayer/Rune Wizard, 4th class skill tree, Ruud shop, Socket Earring, Pentagram).
  - **Client S16 Root Cause & Strategy Comparison**:
    1. `client-s16` (Lgd-Client): Legacy 2021 `main.exe` packed with SCY/Themida; reverse-engineered and patched 8+ crash traps (GetCommandLineA, Null deref, SSE Float, SSO string, UTF-8 converter, texture streamer callback, window event check).
    2. `MuEmu-master` (C# .NET Core Multi-Season Emulator): Verified compiles 100% clean (0 errors). Contains full Season 16 DTO packet serializations (`MU.Network`) and comprehensive client patch definition `ClientConfig/Config_1.19.46.0.ini` (17 crash traps, GameGuard bypass, Floating point, ResourceGuard, Gun Crusher creation).
    3. `MU VoZ SS16.1` (Production Client): Verified complete standalone Season 16.1 production client. Features dynamic INI configuration (`config.ini` with `IpAddress`, `Port`, `Version = 1.07.17`, `Serial = 0hfqckZw30CKOzZA`, `ANTILAG`, `FONT`), pre-matched `Main.dll` (1.09MB) + `CCD.dll` + `DSETUP.dll`, without restrictive proprietary wrapper (`game.bin`/`auth.bin` seen in `ZhyperMU S16 Full`). Designated as the recommended client runtime for the Modern S16 realm.
  - **Evolution Strategy**: Two parallel viable paths: (A) Run native S16 using `MU VoZ SS16.1` client linked to `src/server-s16` C++ server, (B) Port S16 calculation formulas and event state machines from C++ into `MuEmu-master` C# for a modern, cross-platform, multi-season server.










---archived from .claude/memory/MEMORY.md on 2026-08-27---
# Memory Index

> Persistent cross-session memory for THIS project. The AI reads this at
> session start and writes to it via `/remember`. Starts blank — Solo-Code-
> CLI's own accumulated memory is never deployed into target projects.

## Project
- [project] (add your branch naming / workflow conventions here)

## Rules
- [rules] Load AGENTS.md for all behavior rules -> [[AGENTS.md]]

## Decisions
- **Client Data Baseline Sync (2026-08-09)**: Synchronized 100% of Season 6E3 client Data assets (975 files including `Sound/`, `Music/`, `Local/`, `Macro.txt`, map objects) from `MU Client 1.04d - Season 6E3` into `MuMain-main/src/bin/Data` and release output `build/src/Release/Data`.
- **Client Native AOT & UI Fix (2026-08-09)**: Target Framework of `ClientLibrary.csproj` aligned to `net9.0` for OpenMU compatibility; patched character creation UI 2D projection reset (`BeginBitmap()`) in `CharMakeWin.cpp`.
- **S20 Class Shift Fix (2026-08-11)**: Removed 3-bit left shift on the server's `CharacterCreationSuccessful` packet (`ServerToClientPackets.xml`, `.cs`, `.csRef`) and corresponding right shift on the client side (`WSclient.cpp:L761`), fixing the truncation of S20 class numbers (indices >= 32 like Slayer, Lemuria, Rune Wizard) and preventing them from loading/saving as incorrect S6 classes (like Dark Lord).
- **Docker/Client Build Sync (2026-08-11)**: Rebuilt the server startup image `openmu-startup` and updated the client binary `src/bin/Main.exe` with the class shift fix.
- **⚠️ CRITICAL: RUN THE GAME FROM `build/src/Release`, NOT `src/bin` (2026-08-13)**. There are TWO `Main.exe` in the repo and they get out of sync. The authoritative one is `MuMain-main/build/src/Release/Main.exe` (produced by MSBuild, with all fixes). `MuMain-main/src/bin/Main.exe` is a stale copy (last touched 2026-08-11) and does NOT contain any S20 fixes. Always rebuild+run from `build/src/Release`. The build pipeline copies assets from `src/bin` → `build/src/Release` automatically on every Main.vcxproj build.
- **S20 Character Creation Root Cause (2026-08-13)**: Character creation saved wrong classes (e.g. Alchemist→RuneWizard=32, Illusion→DarkLord=16). Root cause: `CreateCharacter` packet's `Class` field is defined with `<LeftShifted>2</LeftShifted>` + `<Length>6</Length>`, so the generated setter `CreateCharacterRef.Class` ALREADY does `<< 2` + 6-bit mask internally. The old client code `(CharacterView.Class << 2)` shifted TWICE. Fix: added `CCharacterManager::ClientClassTypeToServerClassNumber(CLASS_TYPE)` (returns raw class number 0..56 as `BYTE`, in `CharacterManager.h/.cpp`) and `CharMakeWin.cpp:429` now calls it and casts to `CharacterClassNumber`. Do NOT re-add any manual `<< 2` in the create path.
- **Server AppearanceSerializer Version Gates (2026-08-13)**: Fixed plugin selection so the raw-byte Extended serializer is used for S6+ clients. `AppearanceSerializerExtended.cs` `MinimumClient(106,3)` → `(6,3)`; `AppearanceSerializer.cs` added `MaximumClient(5,0)`; `AppearanceSerializer095.cs` added `MaximumClient(0,97)`; `AppearanceSerializer075.cs` added `MaximumClient(0,89)`; `ShowCharacterListExtendedPlugIn.cs` `MinimumClient(106,3)` → `(6,3)`. Client version `20404` resolves to `(106,3)`, `10404` to `(6,3)`.
- **Author's `update/` is S6E3 vanilla (2026-08-13)**: `D:\Project\mu\update\{MuMain-main,OpenMU-master}` is the author's original S6E3 codebase — NO S20 support (MAX_CLASS=7, CharacterClassNumber ends at FistMaster=25). All S20 support is custom-built. The REAL S20 client is `[MUDevs Emulator] Base Client S20.2-3` and `[MUDevs Emulator - FREE] FULL Base Season 20.2-3`.
- **S20 Data completeness (2026-08-13)**: `NewFace08-15.bmd` are REAL S20 models (identical byte-size to `[MUDevs Emulator] Base Client S20.2-3\Data\Logo`). Player/ = 1144 files (S20: 1143) ✅; Skill/ = 633 (S20: 633) ✅; Monster/ = 2145 (S20: 2157) ~✅; **Item/ = 3747 vs S20 8426 — MISSING ~4700 item files** (weapons/equipment). To reach FULL S20, copy `Item/` from the S20 client into `src/bin/Data/Item`.
- **Docker image optimization (2026-08-13)**: Server image reduced 388MB → 374MB by stripping `*.pdb` and `Microsoft.CodeAnalysis*.dll` in `src/Startup/Dockerfile`. DO NOT delete `*.xml` or `*.staticwebassets.*.json` — those are required at runtime (ConnectionSettings.xml is config, staticwebassets manifest is needed by AdminPanel).
- **S20 FULLY REVERTED → S6E3 (2026-08-13) — DECISION**: Abandoned Season 20 (and S9/S13-14) and reverted the codebase back to pure Season 6E3. Reason: the S6E3 client's BMD loader (`ZzzBMD.cpp`) only supports model versions 0x0A/0x0C/0x0E; S20 uses BMD version 0x0F (and S9's newer files use 0x0E) with a different/unknown encryption key — the `webzen#@!01...` comment key does NOT decrypt v14 (tested 4 variants, all failed). Result: 76% of S20 Item models, ~36% Monster, 100% Object1 maps would be invisible. **Max practical season is S6E3** unless a community-sourced v14/v15 codec is found. S20 data source folders (`[MUDevs Emulator] Base Client S20.2-3`, `[MUDevs Emulator - FREE] FULL Base Season 20.2-3`) and `muss9` may still exist on disk.
- **S20 revert scope (2026-08-13) — what was cleaned**: (1) Client: 12 files reverted from `update/MuMain-main` (`_enum.h`, `_define.h`, `CharacterManager.cpp/h`, `CharMakeWin.cpp/h`, `PacketFunctions_CommonEnums.h`, `WSclient.h/cpp`, `ZzzCharacter.cpp`, `ZzzObject.cpp`, `ZzzOpenData.cpp`). (2) Server: deleted 22 files (8 `Class*.cs` + 11 `AddLanceFor*/AddMagicalShotFor*/AddClash*/AddCrescentMoonSlashFor*`), reverted `CharacterClassNumber.cs`, `CharacterClasses.cs`, `CharacterClassHelper.cs`, `CharacterClassInitialization.cs`, `CommonEnums.xml/cs`, `SkillNumber.cs`, `MasterSkillTree.xml`, 4 serializers + `ShowCharacterListExtendedPlugIn.cs`, and `SkillsInitializer.cs` (CrescentMoonSlash→AllKnights, Lance→SoulMasterAndGrandMaster|AllSummoners, removed MagicalShot+Clash). (3) DB: deleted 53 S20 characters, 8 `AccountCharacterClass` rows, 23 `config.CharacterClass` (number>=28); only 18 S6 classes (0-25) remain.
- **⚠️ PRESERVE — user's BOT AI + legit S6 features (do NOT treat as S20 garbage)**: `GameLogic/Bots/*` (16 files), `GameLogic/PlugIns/BotAI/*`, `ChatCommands/AddBotsChatCommandPlugIn.cs`, `VersionSeasonSix/RuudMerchantStore.cs`, `VersionSeasonSix/Maps/*` (16 maps: Acheron, Deventer, Urk Mountain, Nars, Ferea, Nixies Lake, DeepDungeon, Swamp of Darkness/Doom, Kubera Mine, Atlans Abyss), AdminPanel `BotDashboardController.cs`/`Bots.razor`/`BotLiveDto.cs`, Castle Siege migration, Resets system. `MoveCommandData.cpp` maps were relabeled S20→S6 (they are S6 maps).
- **Author `update/` diff = refactor only, NOT applied (2026-08-13) — DECISION**: The only files the author has that current lacks are (1) 9 `GameLogic/*Extensions.cs` split out of the 2621-line `Player.cs` monolith, and (2) 7 `Web/Shared/Components/Toast/*` files replacing the `Blazored.Toast` NuGet package with a self-written toast. Both are **refactors with zero functional benefit** — the same logic already lives inside current `Player.cs`, and `Blazored.Toast` v4.2.1 already works in AdminPanel. Decision: do NOT apply — splitting a 2600-line file offers no value for a solo developer not actively editing `Player.cs`, and swapping a working dependency introduces risk for no gain. Revisit only if `Player.cs` grows >~4000 lines, is edited frequently, or a second developer/AI works on it in parallel.
- **Player website REBUILT as OpenMU.PlayerWeb (2026-08-13) — DECISION**: Rewrote the player-facing website (old `open-mu-web-master` Next.js 14, 1.61GB image) as a Blazor Web App (.NET 9) in `OpenMU.PlayerWeb/`, replacing the Next.js `openmu-web` service in docker-compose. Stack: Static SSR + Interactive Server (forms only), Cookie auth (self-written), EF Core + Npgsql mapping directly into the OpenMU PostgreSQL schemas (config/data/guild), BCrypt.Net-Next (work factor 11 to match OpenMU `$2a$11$`). Design follows the `template/` "Blood & Gold Eternal" dark-fantasy system (Newsreader/Work Sans/JetBrains Mono, #8B0000 blood red + #FFD700 gold, sharp edges, btn-ornate/panel-metallic/rank-table). New image is 339MB (vs 1.61GB). Port 3007. Services: AccountService, CharacterService (reset/addstats/pkclear/resetStats — same stat/map UUIDs as the old Next.js), RankingService, NewsService (data.OpenMuWeb_News), GuildService, ServerStatusService (proxies GAMESERVER_URL/api/status), BotService. Pages: Home, Classes, Maps, Rankings (4 tabs), News, NewsDetail, Download, Info, Terms, Account (index + change password), Login, Register, Logout, Admin/News, Admin/Bots (GM role = CharacterStatus 32). Template images are Google placeholders (will expire — replace with real server art later). **`open-mu-web-master/` (old Next.js) has been DELETED.**
- **⚠️ Tailwind CDN required for PlayerWeb (2026-08-13)**: The Razor pages use Tailwind utility classes (flex/grid/gap/py-*/btn-ornate/etc). The site initially rendered as unstyled text because Tailwind was never loaded. `Components/App.razor` now loads `https://cdn.tailwindcss.com` + a `tailwind.config` with the full "Blood & Gold" color palette (surface/primary/secondary tokens). If layout ever breaks again, check that the CDN script + config are still in App.razor's <head>.
- **⚠️ CRITICAL: DO NOT delete Microsoft.CodeAnalysis*.dll from the server image (2026-08-13)**: OpenMU compiles its plugins at runtime with Roslyn (`Microsoft.CodeAnalysis.CSharp`). A previous image "optimization" stripped `Microsoft.CodeAnalysis*.dll` (thinking it was build-only), which broke bot generation and ALL dynamic plugins — the server logged `FileNotFoundException: Could not load file or assembly 'Microsoft.CodeAnalysis.CSharp, Version=4.14.0.0'` and bots created accounts but never spawned characters (51/100 bot accounts were char-less). Fixed by removing that `find -delete` line from `src/Startup/Dockerfile`. Only `*.pdb` is safe to strip. The image stays ~374MB with Roslyn included.
- **Bot name generator is now a CURATED LIST, not procedural (2026-08-13)**: `GameLogic/Bots/BotNameGenerator.cs` was rewritten to draw from a fixed 698-name pool (LOTR/GOT/Witcher/Star Wars/Star Trek/Warcraft/Dune/MU-themed) instead of procedural syllable concatenation. All names satisfy MU's `CharacterNameRegex` (`^[a-zA-Z0-9]{3,10}$`, set in `GameConfigurationInitializerBase.cs:55` + DB `config.GameConfiguration`). Names >10 chars were truncated (e.g. GrandMaster→GrandMaste). The regex limit of 10 is vanilla MU — do NOT raise it casually (client S6E3 truncates long names).
- **Phases 0-5 Upgrade Implementation & Verification (2026-08-13)**: Completed Phase 0 (clean stale src/bin, activate StatReset), Phase 1 & 2 (activate 8 GM ChatCommands, 3 Invasions, 3 MiniGame commands, DB migrations), Phase 5 & 3 Web Portal (`Rename.razor`, `CharacterService.RenameAsync`, `VipService`, `WheelService`, `LuckyWheel.razor`, `Admin/Vip.razor`, `Account/Index.razor`), and Phase 4 Client C++ (`DiscordRPC.h/cpp` via Win32 named pipe IPC, wired into `Winmain.cpp`, `MainScene.cpp`, `SceneManager.cpp`). Both `OpenMU.PlayerWeb` and `Main.exe` built cleanly with 0 errors. Verified 15/15 plugin GUIDs in PostgreSQL (`IsActive=true`). Rebuilt and deployed `mu-openmu-playerweb` Docker container to port 3007.
- **Blazor Cookie Authentication & News Table Fix (2026-08-13)**: Fixed `Sign in` failing on `OpenMU.PlayerWeb`. Root cause 1: `Login.razor` previously ran under `@rendermode InteractiveServer` over a WebSocket SignalR circuit where `HttpContext.SignInAsync` cannot write HTTP response cookies (headers already sent). Solution: Mapped standard HTTP POST `/api/auth/login` and GET `/logout` endpoints in `Program.cs` and converted `Login.razor` to a Static SSR form submitting to `/api/auth/login`. Root cause 2: `data."OpenMuWeb_News"` table was missing in PostgreSQL causing unhandled exception on Home/Redirect. Solution: Created table `data."OpenMuWeb_News"` and inserted default welcome post. Web portal now returns HTTP 200 OK across all pages.
- **Server-side VIP EXP Bonus Implementation (2026-08-13)**: Added `VipTier` and `VipExpiry` properties to `OpenMU-master/src/DataModel/Entities/Account.cs`. Added `GetVipExpMultiplier()` method to `Player.cs` (Silver=1.2x, Gold=1.5x, Platinum=2.0x when active). Applied `GetVipExpMultiplier()` inside `Player.cs` (`CalculateExpAfterKill`) and `Party.cs` (`DistributeExperienceToPartyMemberAsync`). Built `Startup.csproj` (0 errors) and redeployed `openmu-server` Docker container.
- **Bot Personality Dashboard Upgrade (2026-08-13)**: Upgraded AdminPanel Bot AI Dashboard (`http://localhost:8081/bots`). Resolved real personality archetypes (`BotPersonalityResolver.Resolve`) for live bots (`Balanced`, `Greedy`, `Warrior`, `Loner`, `Guardian`, `Reckless`). Added Personality Breakdown stats bar, Top Farmer metric, and Personality Filter dropdown to UI. Rebuilt `MUnique.OpenMU.Startup.csproj` (0 errors) and redeployed `openmu-server` Docker container.
- **Bot AI System Consolidation & Humanized Chat (2026-08-13)**: Audited dual bot systems (`GameLogic/Bots` vs `GameLogic/PlugIns/BotAI`). Verified `GameLogic/Bots` is active source of truth. Created `BotChatHandler.cs` (IChatMessageReceivedPlugIn) to give bots natural, intent-based chat responses without fake promises (integrated with `BotPartyHandler` for real party requests). Broadcasts chat overhead to all nearby map observers (`ForEachWorldObserverAsync<IChatViewPlugIn>`). Includes TTL-pruned rate limiting and proactive death reactions (`BotRevengePlugIn.cs`). Marked `SmartBotFeaturePlugIn.cs` as Obsolete. Added description metadata to `AddBotsChatCommandPlugIn.cs`. 100% of all 719 unit tests passed, 0 build errors. Report written to `.gemini/antigravity/handoff/outbox/bot-ai-consolidation-report.md`.
- **Client Codebase Audit & Data Asset Synchronization (2026-08-13)**: Audited `MuMain-main/src` against reference repo `update/MuMain-main/src`. Confirmed codebase is 100% clean Season 6 Episode 3 standard with ONLY `DiscordRPC.h/cpp` added. Resolved CMake VS2022 output path (`build/Release/Main.exe`). Performed full clean build of Client `Main.exe` (Timestamp: 8:25:33 PM) with watermark tag `=== S6E3 CLEAN CLIENT ===`. Synced clean binary across all execution paths (`build/Release`, `build/src/Release`, `src/Main.exe`, `src/bin/Main.exe`).
- **Part A Simulator Final Calibration Verified (2026-08-17)**: Replaced hardcoded monster selector with dynamic LINQ ranking based on unpenalized EXP/h (`ScoreMonster`), generated diagnostic `monster_coverage_report.csv` (correctly identifying dominated Coolutin #457 at L240), verified Zen Cap All-or-Nothing overflow unit test, added `ResetPaid` blocked flags to Scenarios A/C2/D2/F/GoldPreset, and regenerated all 15 CSV files with 100% acceptance gates passed (8/8 PASS). Source code audit frozen.
- **Simulator V3 Specification & Gate Audit Handoff Documented (2026-08-18)**: Saved complete Round 3 specifications (Gate level audit from `Gates.cs` [Raklion=280, KanturuRelics=230, etc.], per-kill Zen cap granularity, `ResetPaid` execution control, stale CSV cleanup, and 9 acceptance criteria) in `SIMULATOR_V3_SPECIFICATION_HANDOFF.md` to revisit after Season 16 evolution.
- **Season 16 Full Asset Ecosystem & Technical Specification Available (2026-08-18)**: Added complete Season 16 package in `sourecodeadd/` consisting of (1) `ZhyperMU S16 Full` (100% client assets, 162 subdirectories, 137 3D maps, items, monsters, sounds), (2) `MuClientTools16` (1,544 tool files & standalone native .exe decoders for all S16 BMD/ATT/OZ* formats), (3) `Server - LgdMu s16 1.1` & `Lgd-Server-main` (Full C++ GameServer/ConnectServer/LoginServer + MySQL DBs, complete packet maps in `ClientPacket.h`, mix formulas, elemental pentagram, Muun pet, Majestic skill tree), (4) `Client - LgdMu S16 1.1` (C++ `Main.dll` v1.1 hook with auto-reconnect, anti-cheat, custom objects), (5) `MuOnline-WorldEditor-master` (C++ ImGui realtime map editor). Decision: This provides both a standalone S16 environment and a ground-truth specification to optionally upgrade OpenMU C# or downport assets.
- **S16 Server Restored to Runbook State (2026-08-18) — DECISION**: Restored the S16 runtime after repo restructure. Three root causes fixed to get `GameServer.exe` booting: (1) **UTF-8 BOM** in the 5 root `.conf` files under `D:\Project\mutopia\server-s16\` broke `boost::property_tree::ini_parser` → section name parsed as `\xEF\xBB\xBF[SERVER]` so every key (incl. `GameServer.Database.Info`) read empty → "GameServer database not specified". Stripped BOM from all 5. (2) **`server_list` schema wrong** — Gemini's auto-generated table lacked column `display_id`, so ConnectServer's index-based `LoadServerList()` (reads `fields[0..10]`) shifted every column. Recreated 11-column table + inserted `flag=3`. (3) **missing `libmysql.dll` in `GameServer\`** — GameServer crashed mid-handshake; copying `libmysql.dll` from root into `GameServer\` fixed it. After these, GameServer reached `GameServer Ready` + all 3 internal handshakes (SERVERLINK 55555, AUTHSERVER 44417, CONNECTSERVER 44419).
- **S16 GameServer crash #2 — RESOLVED (2026-08-18)**: Root cause = **`monster_template` table empty (0 rows)** while `monster` spawn table had 10,220 rows referencing template ids. When GameServer spawned monsters from `monster`, `MonsterManager::LoadMonsterTemplate()` (`Game/MonsterManager.cpp:65`) found no template → null deref in a Boost.Asio worker thread → hard access violation (not caught by UpdateLoop's try/catch, hence no "FATAL EXCEPTION" line). The crash "moved" (19s→39s) as more data was loaded because more async work happened. **Fix = re-import `Databases\Game\monster - complete.sql` LAST** (it contains 786 `INSERT monster_template` + 10,220 `INSERT monster`). ⚠️ **Import-order trap**: `monster_template.sql` (3.3KB) is schema-only with `DROP TABLE` — importing it AFTER `monster - complete.sql` wipes the 786 templates. The 18 `[DIAG2]` log markers are still in `Game/Main.cpp` (harmless, leave them). After fix: `Loaded 786 monsters definitions`, tick passed 1405+, GameServer stable >2min, port 55902 listening. The "High Monster GUID" ERROR lines are benign (informational spawn-guid notices, not fatal).
- **S16 Part B data loaded (2026-08-18)**: Imported `monster - complete.sql` → `monster`=10,220, `monster_template`=786, `monster_skill`=342, `monster_skill_special`=160, `monster_ai_automata`=164, `monster_ai_unit`=47, `monster_ai_group`=88, `monster_respawn_location`=33, `event_manager`=362. Client runtime copied to `D:\Project\mutopia\`: `client-s16\` (30,584 files / 2.12 GB — Data + main.exe 25.5MB), `client-s6\` (19,952 files / 1.57 GB — Data + Main.exe), `launcher\` (MuLauncher.exe + servers.json). S16 client exe = `main.exe` (25.5MB) from `reference\s16-data\MuOnline_S16_Lgd-main\Main\`; S6 = `Main.exe` from `src\client-s6\src\bin\`.
- **⚠️ S6 client authoritative binary GONE (2026-08-18)**: restructure DELETED `MuMain-main/build/` (3.5 GB) which held the authoritative `build/src/Release/Main.exe`. Only `src/client-s6/src/bin/Main.exe` remains (stale 2026-08-11). Rebuild via CMake/MSBuild when a fresh client binary is needed.
- **S16 Server Restored to Runbook State (2026-08-18) — DECISION**: Restored the S16 runtime after repo restructure. Three root causes fixed to get `GameServer.exe` booting: (1) **UTF-8 BOM** in the 5 root `.conf` files under `D:\Project\mutopia\server-s16\` (`connectserver.conf`, `gameserver.conf`, `game_common.conf`, `loginserver.conf`, `serverlink.conf`) broke `boost::property_tree::ini_parser` — it parsed section name as `\xEF\xBB\xBF[SERVER]` so every key (incl. `GameServer.Database.Info`) read empty → "GameServer database not specified". Stripped BOM from all 5. (2) **`server_list` schema wrong** — Gemini's auto-generated table lacked column `display_id`, so ConnectServer's index-based `LoadServerList()` (reads `fields[0..10]` = code, display_id, name, port, ip, flag, online, default_world, default_x, default_y, type) shifted every column → log showed `Port: 127 IP: 1 Disabled: 1`. Recreated the 11-column table + inserted `flag=3` (DISPLAY|ACCOUNT_ALLOWED, no DISABLED). (3) **missing `libmysql.dll` in `GameServer\`** — GameServer crashed mid-handshake; copying `libmysql.dll` from root into `GameServer\` fixed it. After these, GameServer reached `GameServer Ready` + all 3 internal handshakes (SERVERLINK 55555, AUTHSERVER 44417, CONNECTSERVER 44419) succeeded. Remaining open issue: GameServer crashes at `Loading Castle Siege NPC From ServerLink` — likely a struct-layout/packet mismatch because the 4 binaries were built at different times (GameServer 14:21 vs others 13:41); fix = rebuild all 4 from one consistent build. **S16 stack: MySQL 5.6 Docker (port 3307) with 4 DBs (`database_login`=16 tbl, `database_game`=179, `database_characters`=97, `database_log`=21); runtime binaries + conf in `D:\Project\mutopia\server-s16\`; start order = LoginServer → ConnectServer → ServerLink → GameServer.**
- **Season 16 Multi-Realm Architecture & Client Strategy Assessment (2026-08-19) — DECISION**:
  - **Server S16 Native (`src/server-s16` - C++ MSVC Win32)**: Verified 100% compiled clean with 0 errors (`GameServer.exe`, `ConnectServer.exe`, `DataServer.exe`), schema validated on SQL Server 2022 (63 tables + Castle Siege, Character, Item data). Full Season 16 business logic (10 classes, Gun Crusher/Slayer/Rune Wizard, 4th class skill tree, Ruud shop, Socket Earring, Pentagram).
  - **Client S16 Root Cause & Strategy Comparison**:
    1. `client-s16` (Lgd-Client): Legacy 2021 `main.exe` packed with SCY/Themida; reverse-engineered and patched 8+ crash traps (GetCommandLineA, Null deref, SSE Float, SSO string, UTF-8 converter, texture streamer callback, window event check).
    2. `MuEmu-master` (C# .NET Core Multi-Season Emulator): Verified compiles 100% clean (0 errors). Contains full Season 16 DTO packet serializations (`MU.Network`) and comprehensive client patch definition `ClientConfig/Config_1.19.46.0.ini` (17 crash traps, GameGuard bypass, Floating point, ResourceGuard, Gun Crusher creation).
    3. `MU VoZ SS16.1` (Production Client): Verified complete standalone Season 16.1 production client. Features dynamic INI configuration (`config.ini` with `IpAddress`, `Port`, `Version = 1.07.17`, `Serial = 0hfqckZw30CKOzZA`, `ANTILAG`, `FONT`), pre-matched `Main.dll` (1.09MB) + `CCD.dll` + `DSETUP.dll`, without restrictive proprietary wrapper (`game.bin`/`auth.bin` seen in `ZhyperMU S16 Full`). Designated as the recommended client runtime for the Modern S16 realm.
- **Hybrid Milestone Reset Model & Clean DB Calibration (2026-08-21) — DECISION**:
  - **Gameplay Model**: Adopted verified Hybrid Milestone Reset Model combining early-game player retention with mid/late-game AFK stability.
    - *Stage 1 (Resets 1-15)*: Progressive level requirement $\text{ReqLevel} = \min(400, 50 + (R-1) \times 20)$ (Lv 50 $\rightarrow$ 330). Resets 1-3 are **0 Zen** (Free starter assistance); Resets 4-5 = 200k-400k Zen; Resets 6-15 = 1M-6M Zen. Points rewarded: $+250 + (R-1) \times 50$ (Total: 9,000 Points).
    - *Stage 2 (Resets 16-50)*: Fixed Level 400 requirement. Phí: 10M + $(R-15) \times 1\text{M}$ Zen. Points rewarded: $+1,000$ points/reset (Total: 44,000 Points at 50 resets, avoiding 65k agility animation breaks).
  - **Codebase Calibration**: Updated [ResetProgressionCalculator.cs](file:///f:/Project/mu/src/server-s6/src/GameLogic/Resets/ResetProgressionCalculator.cs) and verified `/reset` ingame command ([ResetChatCommandPlugIn.cs](file:///f:/Project/mu/src/server-s6/src/GameLogic/Resets/ResetChatCommandPlugIn.cs)). Clean compilation confirmed across solution (`0 Errors`).
  - **Specification Documentation**: Documented in [EXP_AND_DROP_CALIBRATION_SPEC.md](file:///f:/Project/mu/docs/EXP_AND_DROP_CALIBRATION_SPEC.md).










---archived from .kilo/memory/MEMORY.md on 2026-08-27---
# Memory Index

> Persistent cross-session memory for THIS project. The AI reads this at
> session start and writes to it via `/remember`. Starts blank — Solo-Code-
> CLI's own accumulated memory is never deployed into target projects.

## Project
- [project] (add your branch naming / workflow conventions here)

## Rules
- [rules] Load AGENTS.md for all behavior rules -> [[AGENTS.md]]

## Decisions
- **Client Data Baseline Sync (2026-08-09)**: Synchronized 100% of Season 6E3 client Data assets (975 files including `Sound/`, `Music/`, `Local/`, `Macro.txt`, map objects) from `MU Client 1.04d - Season 6E3` into `MuMain-main/src/bin/Data` and release output `build/src/Release/Data`.
- **Client Native AOT & UI Fix (2026-08-09)**: Target Framework of `ClientLibrary.csproj` aligned to `net9.0` for OpenMU compatibility; patched character creation UI 2D projection reset (`BeginBitmap()`) in `CharMakeWin.cpp`.
- **S20 Class Shift Fix (2026-08-11)**: Removed 3-bit left shift on the server's `CharacterCreationSuccessful` packet (`ServerToClientPackets.xml`, `.cs`, `.csRef`) and corresponding right shift on the client side (`WSclient.cpp:L761`), fixing the truncation of S20 class numbers (indices >= 32 like Slayer, Lemuria, Rune Wizard) and preventing them from loading/saving as incorrect S6 classes (like Dark Lord).
- **Docker/Client Build Sync (2026-08-11)**: Rebuilt the server startup image `openmu-startup` and updated the client binary `src/bin/Main.exe` with the class shift fix.
- **⚠️ CRITICAL: RUN THE GAME FROM `build/src/Release`, NOT `src/bin` (2026-08-13)**. There are TWO `Main.exe` in the repo and they get out of sync. The authoritative one is `MuMain-main/build/src/Release/Main.exe` (produced by MSBuild, with all fixes). `MuMain-main/src/bin/Main.exe` is a stale copy (last touched 2026-08-11) and does NOT contain any S20 fixes. Always rebuild+run from `build/src/Release`. The build pipeline copies assets from `src/bin` → `build/src/Release` automatically on every Main.vcxproj build.
- **S20 Character Creation Root Cause (2026-08-13)**: Character creation saved wrong classes (e.g. Alchemist→RuneWizard=32, Illusion→DarkLord=16). Root cause: `CreateCharacter` packet's `Class` field is defined with `<LeftShifted>2</LeftShifted>` + `<Length>6</Length>`, so the generated setter `CreateCharacterRef.Class` ALREADY does `<< 2` + 6-bit mask internally. The old client code `(CharacterView.Class << 2)` shifted TWICE. Fix: added `CCharacterManager::ClientClassTypeToServerClassNumber(CLASS_TYPE)` (returns raw class number 0..56 as `BYTE`, in `CharacterManager.h/.cpp`) and `CharMakeWin.cpp:429` now calls it and casts to `CharacterClassNumber`. Do NOT re-add any manual `<< 2` in the create path.
- **Server AppearanceSerializer Version Gates (2026-08-13)**: Fixed plugin selection so the raw-byte Extended serializer is used for S6+ clients. `AppearanceSerializerExtended.cs` `MinimumClient(106,3)` → `(6,3)`; `AppearanceSerializer.cs` added `MaximumClient(5,0)`; `AppearanceSerializer095.cs` added `MaximumClient(0,97)`; `AppearanceSerializer075.cs` added `MaximumClient(0,89)`; `ShowCharacterListExtendedPlugIn.cs` `MinimumClient(106,3)` → `(6,3)`. Client version `20404` resolves to `(106,3)`, `10404` to `(6,3)`.
- **Author's `update/` is S6E3 vanilla (2026-08-13)**: `D:\Project\mu\update\{MuMain-main,OpenMU-master}` is the author's original S6E3 codebase — NO S20 support (MAX_CLASS=7, CharacterClassNumber ends at FistMaster=25). All S20 support is custom-built. The REAL S20 client is `[MUDevs Emulator] Base Client S20.2-3` and `[MUDevs Emulator - FREE] FULL Base Season 20.2-3`.
- **S20 Data completeness (2026-08-13)**: `NewFace08-15.bmd` are REAL S20 models (identical byte-size to `[MUDevs Emulator] Base Client S20.2-3\Data\Logo`). Player/ = 1144 files (S20: 1143) ✅; Skill/ = 633 (S20: 633) ✅; Monster/ = 2145 (S20: 2157) ~✅; **Item/ = 3747 vs S20 8426 — MISSING ~4700 item files** (weapons/equipment). To reach FULL S20, copy `Item/` from the S20 client into `src/bin/Data/Item`.
- **Docker image optimization (2026-08-13)**: Server image reduced 388MB → 374MB by stripping `*.pdb` and `Microsoft.CodeAnalysis*.dll` in `src/Startup/Dockerfile`. DO NOT delete `*.xml` or `*.staticwebassets.*.json` — those are required at runtime (ConnectionSettings.xml is config, staticwebassets manifest is needed by AdminPanel).
- **S20 FULLY REVERTED → S6E3 (2026-08-13) — DECISION**: Abandoned Season 20 (and S9/S13-14) and reverted the codebase back to pure Season 6E3. Reason: the S6E3 client's BMD loader (`ZzzBMD.cpp`) only supports model versions 0x0A/0x0C/0x0E; S20 uses BMD version 0x0F (and S9's newer files use 0x0E) with a different/unknown encryption key — the `webzen#@!01...` comment key does NOT decrypt v14 (tested 4 variants, all failed). Result: 76% of S20 Item models, ~36% Monster, 100% Object1 maps would be invisible. **Max practical season is S6E3** unless a community-sourced v14/v15 codec is found. S20 data source folders (`[MUDevs Emulator] Base Client S20.2-3`, `[MUDevs Emulator - FREE] FULL Base Season 20.2-3`) and `muss9` may still exist on disk.
- **S20 revert scope (2026-08-13) — what was cleaned**: (1) Client: 12 files reverted from `update/MuMain-main` (`_enum.h`, `_define.h`, `CharacterManager.cpp/h`, `CharMakeWin.cpp/h`, `PacketFunctions_CommonEnums.h`, `WSclient.h/cpp`, `ZzzCharacter.cpp`, `ZzzObject.cpp`, `ZzzOpenData.cpp`). (2) Server: deleted 22 files (8 `Class*.cs` + 11 `AddLanceFor*/AddMagicalShotFor*/AddClash*/AddCrescentMoonSlashFor*`), reverted `CharacterClassNumber.cs`, `CharacterClasses.cs`, `CharacterClassHelper.cs`, `CharacterClassInitialization.cs`, `CommonEnums.xml/cs`, `SkillNumber.cs`, `MasterSkillTree.xml`, 4 serializers + `ShowCharacterListExtendedPlugIn.cs`, and `SkillsInitializer.cs` (CrescentMoonSlash→AllKnights, Lance→SoulMasterAndGrandMaster|AllSummoners, removed MagicalShot+Clash). (3) DB: deleted 53 S20 characters, 8 `AccountCharacterClass` rows, 23 `config.CharacterClass` (number>=28); only 18 S6 classes (0-25) remain.
- **⚠️ PRESERVE — user's BOT AI + legit S6 features (do NOT treat as S20 garbage)**: `GameLogic/Bots/*` (16 files), `GameLogic/PlugIns/BotAI/*`, `ChatCommands/AddBotsChatCommandPlugIn.cs`, `VersionSeasonSix/RuudMerchantStore.cs`, `VersionSeasonSix/Maps/*` (16 maps: Acheron, Deventer, Urk Mountain, Nars, Ferea, Nixies Lake, DeepDungeon, Swamp of Darkness/Doom, Kubera Mine, Atlans Abyss), AdminPanel `BotDashboardController.cs`/`Bots.razor`/`BotLiveDto.cs`, Castle Siege migration, Resets system. `MoveCommandData.cpp` maps were relabeled S20→S6 (they are S6 maps).
- **Author `update/` diff = refactor only, NOT applied (2026-08-13) — DECISION**: The only files the author has that current lacks are (1) 9 `GameLogic/*Extensions.cs` split out of the 2621-line `Player.cs` monolith, and (2) 7 `Web/Shared/Components/Toast/*` files replacing the `Blazored.Toast` NuGet package with a self-written toast. Both are **refactors with zero functional benefit** — the same logic already lives inside current `Player.cs`, and `Blazored.Toast` v4.2.1 already works in AdminPanel. Decision: do NOT apply — splitting a 2600-line file offers no value for a solo developer not actively editing `Player.cs`, and swapping a working dependency introduces risk for no gain. Revisit only if `Player.cs` grows >~4000 lines, is edited frequently, or a second developer/AI works on it in parallel.
- **Player website REBUILT as OpenMU.PlayerWeb (2026-08-13) — DECISION**: Rewrote the player-facing website (old `open-mu-web-master` Next.js 14, 1.61GB image) as a Blazor Web App (.NET 9) in `OpenMU.PlayerWeb/`, replacing the Next.js `openmu-web` service in docker-compose. Stack: Static SSR + Interactive Server (forms only), Cookie auth (self-written), EF Core + Npgsql mapping directly into the OpenMU PostgreSQL schemas (config/data/guild), BCrypt.Net-Next (work factor 11 to match OpenMU `$2a$11$`). Design follows the `template/` "Blood & Gold Eternal" dark-fantasy system (Newsreader/Work Sans/JetBrains Mono, #8B0000 blood red + #FFD700 gold, sharp edges, btn-ornate/panel-metallic/rank-table). New image is 339MB (vs 1.61GB). Port 3007. Services: AccountService, CharacterService (reset/addstats/pkclear/resetStats — same stat/map UUIDs as the old Next.js), RankingService, NewsService (data.OpenMuWeb_News), GuildService, ServerStatusService (proxies GAMESERVER_URL/api/status), BotService. Pages: Home, Classes, Maps, Rankings (4 tabs), News, NewsDetail, Download, Info, Terms, Account (index + change password), Login, Register, Logout, Admin/News, Admin/Bots (GM role = CharacterStatus 32). Template images are Google placeholders (will expire — replace with real server art later). **`open-mu-web-master/` (old Next.js) has been DELETED.**
- **⚠️ Tailwind CDN required for PlayerWeb (2026-08-13)**: The Razor pages use Tailwind utility classes (flex/grid/gap/py-*/btn-ornate/etc). The site initially rendered as unstyled text because Tailwind was never loaded. `Components/App.razor` now loads `https://cdn.tailwindcss.com` + a `tailwind.config` with the full "Blood & Gold" color palette (surface/primary/secondary tokens). If layout ever breaks again, check that the CDN script + config are still in App.razor's <head>.
- **⚠️ CRITICAL: DO NOT delete Microsoft.CodeAnalysis*.dll from the server image (2026-08-13)**: OpenMU compiles its plugins at runtime with Roslyn (`Microsoft.CodeAnalysis.CSharp`). A previous image "optimization" stripped `Microsoft.CodeAnalysis*.dll` (thinking it was build-only), which broke bot generation and ALL dynamic plugins — the server logged `FileNotFoundException: Could not load file or assembly 'Microsoft.CodeAnalysis.CSharp, Version=4.14.0.0'` and bots created accounts but never spawned characters (51/100 bot accounts were char-less). Fixed by removing that `find -delete` line from `src/Startup/Dockerfile`. Only `*.pdb` is safe to strip. The image stays ~374MB with Roslyn included.
- **Bot name generator is now a CURATED LIST, not procedural (2026-08-13)**: `GameLogic/Bots/BotNameGenerator.cs` was rewritten to draw from a fixed 698-name pool (LOTR/GOT/Witcher/Star Wars/Star Trek/Warcraft/Dune/MU-themed) instead of procedural syllable concatenation. All names satisfy MU's `CharacterNameRegex` (`^[a-zA-Z0-9]{3,10}$`, set in `GameConfigurationInitializerBase.cs:55` + DB `config.GameConfiguration`). Names >10 chars were truncated (e.g. GrandMaster→GrandMaste). The regex limit of 10 is vanilla MU — do NOT raise it casually (client S6E3 truncates long names).
- **Phases 0-5 Upgrade Implementation & Verification (2026-08-13)**: Completed Phase 0 (clean stale src/bin, activate StatReset), Phase 1 & 2 (activate 8 GM ChatCommands, 3 Invasions, 3 MiniGame commands, DB migrations), Phase 5 & 3 Web Portal (`Rename.razor`, `CharacterService.RenameAsync`, `VipService`, `WheelService`, `LuckyWheel.razor`, `Admin/Vip.razor`, `Account/Index.razor`), and Phase 4 Client C++ (`DiscordRPC.h/cpp` via Win32 named pipe IPC, wired into `Winmain.cpp`, `MainScene.cpp`, `SceneManager.cpp`). Both `OpenMU.PlayerWeb` and `Main.exe` built cleanly with 0 errors. Verified 15/15 plugin GUIDs in PostgreSQL (`IsActive=true`). Rebuilt and deployed `mu-openmu-playerweb` Docker container to port 3007.
- **Blazor Cookie Authentication & News Table Fix (2026-08-13)**: Fixed `Sign in` failing on `OpenMU.PlayerWeb`. Root cause 1: `Login.razor` previously ran under `@rendermode InteractiveServer` over a WebSocket SignalR circuit where `HttpContext.SignInAsync` cannot write HTTP response cookies (headers already sent). Solution: Mapped standard HTTP POST `/api/auth/login` and GET `/logout` endpoints in `Program.cs` and converted `Login.razor` to a Static SSR form submitting to `/api/auth/login`. Root cause 2: `data."OpenMuWeb_News"` table was missing in PostgreSQL causing unhandled exception on Home/Redirect. Solution: Created table `data."OpenMuWeb_News"` and inserted default welcome post. Web portal now returns HTTP 200 OK across all pages.
- **Server-side VIP EXP Bonus Implementation (2026-08-13)**: Added `VipTier` and `VipExpiry` properties to `OpenMU-master/src/DataModel/Entities/Account.cs`. Added `GetVipExpMultiplier()` method to `Player.cs` (Silver=1.2x, Gold=1.5x, Platinum=2.0x when active). Applied `GetVipExpMultiplier()` inside `Player.cs` (`CalculateExpAfterKill`) and `Party.cs` (`DistributeExperienceToPartyMemberAsync`). Built `Startup.csproj` (0 errors) and redeployed `openmu-server` Docker container.
- **Bot Personality Dashboard Upgrade (2026-08-13)**: Upgraded AdminPanel Bot AI Dashboard (`http://localhost:8081/bots`). Resolved real personality archetypes (`BotPersonalityResolver.Resolve`) for live bots (`Balanced`, `Greedy`, `Warrior`, `Loner`, `Guardian`, `Reckless`). Added Personality Breakdown stats bar, Top Farmer metric, and Personality Filter dropdown to UI. Rebuilt `MUnique.OpenMU.Startup.csproj` (0 errors) and redeployed `openmu-server` Docker container.
- **Bot AI System Consolidation & Humanized Chat (2026-08-13)**: Audited dual bot systems (`GameLogic/Bots` vs `GameLogic/PlugIns/BotAI`). Verified `GameLogic/Bots` is active source of truth. Created `BotChatHandler.cs` (IChatMessageReceivedPlugIn) to give bots natural, intent-based chat responses without fake promises (integrated with `BotPartyHandler` for real party requests). Broadcasts chat overhead to all nearby map observers (`ForEachWorldObserverAsync<IChatViewPlugIn>`). Includes TTL-pruned rate limiting and proactive death reactions (`BotRevengePlugIn.cs`). Marked `SmartBotFeaturePlugIn.cs` as Obsolete. Added description metadata to `AddBotsChatCommandPlugIn.cs`. 100% of all 719 unit tests passed, 0 build errors. Report written to `.gemini/antigravity/handoff/outbox/bot-ai-consolidation-report.md`.
- **Client Codebase Audit & Data Asset Synchronization (2026-08-13)**: Audited `MuMain-main/src` against reference repo `update/MuMain-main/src`. Confirmed codebase is 100% clean Season 6 Episode 3 standard with ONLY `DiscordRPC.h/cpp` added. Resolved CMake VS2022 output path (`build/Release/Main.exe`). Performed full clean build of Client `Main.exe` (Timestamp: 8:25:33 PM) with watermark tag `=== S6E3 CLEAN CLIENT ===`. Synced clean binary across all execution paths (`build/Release`, `build/src/Release`, `src/Main.exe`, `src/bin/Main.exe`).
- **Part A Simulator Final Calibration Verified (2026-08-17)**: Replaced hardcoded monster selector with dynamic LINQ ranking based on unpenalized EXP/h (`ScoreMonster`), generated diagnostic `monster_coverage_report.csv` (correctly identifying dominated Coolutin #457 at L240), verified Zen Cap All-or-Nothing overflow unit test, added `ResetPaid` blocked flags to Scenarios A/C2/D2/F/GoldPreset, and regenerated all 15 CSV files with 100% acceptance gates passed (8/8 PASS). Source code audit frozen.
- **Simulator V3 Specification & Gate Audit Handoff Documented (2026-08-18)**: Saved complete Round 3 specifications (Gate level audit from `Gates.cs` [Raklion=280, KanturuRelics=230, etc.], per-kill Zen cap granularity, `ResetPaid` execution control, stale CSV cleanup, and 9 acceptance criteria) in `SIMULATOR_V3_SPECIFICATION_HANDOFF.md` to revisit after Season 16 evolution.
- **Season 16 Full Asset Ecosystem & Technical Specification Available (2026-08-18)**: Added complete Season 16 package in `sourecodeadd/` consisting of (1) `ZhyperMU S16 Full` (100% client assets, 162 subdirectories, 137 3D maps, items, monsters, sounds), (2) `MuClientTools16` (1,544 tool files & standalone native .exe decoders for all S16 BMD/ATT/OZ* formats), (3) `Server - LgdMu s16 1.1` & `Lgd-Server-main` (Full C++ GameServer/ConnectServer/LoginServer + MySQL DBs, complete packet maps in `ClientPacket.h`, mix formulas, elemental pentagram, Muun pet, Majestic skill tree), (4) `Client - LgdMu S16 1.1` (C++ `Main.dll` v1.1 hook with auto-reconnect, anti-cheat, custom objects), (5) `MuOnline-WorldEditor-master` (C++ ImGui realtime map editor). Decision: This provides both a standalone S16 environment and a ground-truth specification to optionally upgrade OpenMU C# or downport assets.
- **S16 Server Restored to Runbook State (2026-08-18) — DECISION**: Restored the S16 runtime after repo restructure. Three root causes fixed to get `GameServer.exe` booting: (1) **UTF-8 BOM** in the 5 root `.conf` files under `D:\Project\mutopia\server-s16\` broke `boost::property_tree::ini_parser` → section name parsed as `\xEF\xBB\xBF[SERVER]` so every key (incl. `GameServer.Database.Info`) read empty → "GameServer database not specified". Stripped BOM from all 5. (2) **`server_list` schema wrong** — Gemini's auto-generated table lacked column `display_id`, so ConnectServer's index-based `LoadServerList()` (reads `fields[0..10]`) shifted every column. Recreated 11-column table + inserted `flag=3`. (3) **missing `libmysql.dll` in `GameServer\`** — GameServer crashed mid-handshake; copying `libmysql.dll` from root into `GameServer\` fixed it. After these, GameServer reached `GameServer Ready` + all 3 internal handshakes (SERVERLINK 55555, AUTHSERVER 44417, CONNECTSERVER 44419).
- **S16 GameServer crash #2 — RESOLVED (2026-08-18)**: Root cause = **`monster_template` table empty (0 rows)** while `monster` spawn table had 10,220 rows referencing template ids. When GameServer spawned monsters from `monster`, `MonsterManager::LoadMonsterTemplate()` (`Game/MonsterManager.cpp:65`) found no template → null deref in a Boost.Asio worker thread → hard access violation (not caught by UpdateLoop's try/catch, hence no "FATAL EXCEPTION" line). The crash "moved" (19s→39s) as more data was loaded because more async work happened. **Fix = re-import `Databases\Game\monster - complete.sql` LAST** (it contains 786 `INSERT monster_template` + 10,220 `INSERT monster`). ⚠️ **Import-order trap**: `monster_template.sql` (3.3KB) is schema-only with `DROP TABLE` — importing it AFTER `monster - complete.sql` wipes the 786 templates. The 18 `[DIAG2]` log markers are still in `Game/Main.cpp` (harmless, leave them). After fix: `Loaded 786 monsters definitions`, tick passed 1405+, GameServer stable >2min, port 55902 listening. The "High Monster GUID" ERROR lines are benign (informational spawn-guid notices, not fatal).
- **S16 Part B data loaded (2026-08-18)**: Imported `monster - complete.sql` → `monster`=10,220, `monster_template`=786, `monster_skill`=342, `monster_skill_special`=160, `monster_ai_automata`=164, `monster_ai_unit`=47, `monster_ai_group`=88, `monster_respawn_location`=33, `event_manager`=362. Client runtime copied to `D:\Project\mutopia\`: `client-s16\` (30,584 files / 2.12 GB — Data + main.exe 25.5MB), `client-s6\` (19,952 files / 1.57 GB — Data + Main.exe), `launcher\` (MuLauncher.exe + servers.json). S16 client exe = `main.exe` (25.5MB) from `reference\s16-data\MuOnline_S16_Lgd-main\Main\`; S6 = `Main.exe` from `src\client-s6\src\bin\`.
- **⚠️ S6 client authoritative binary GONE (2026-08-18)**: restructure DELETED `MuMain-main/build/` (3.5 GB) which held the authoritative `build/src/Release/Main.exe`. Only `src/client-s6/src/bin/Main.exe` remains (stale 2026-08-11). Rebuild via CMake/MSBuild when a fresh client binary is needed.
- **S16 Server Restored to Runbook State (2026-08-18) — DECISION**: Restored the S16 runtime after repo restructure. Three root causes fixed to get `GameServer.exe` booting: (1) **UTF-8 BOM** in the 5 root `.conf` files under `D:\Project\mutopia\server-s16\` (`connectserver.conf`, `gameserver.conf`, `game_common.conf`, `loginserver.conf`, `serverlink.conf`) broke `boost::property_tree::ini_parser` — it parsed section name as `\xEF\xBB\xBF[SERVER]` so every key (incl. `GameServer.Database.Info`) read empty → "GameServer database not specified". Stripped BOM from all 5. (2) **`server_list` schema wrong** — Gemini's auto-generated table lacked column `display_id`, so ConnectServer's index-based `LoadServerList()` (reads `fields[0..10]` = code, display_id, name, port, ip, flag, online, default_world, default_x, default_y, type) shifted every column → log showed `Port: 127 IP: 1 Disabled: 1`. Recreated the 11-column table + inserted `flag=3` (DISPLAY|ACCOUNT_ALLOWED, no DISABLED). (3) **missing `libmysql.dll` in `GameServer\`** — GameServer crashed mid-handshake; copying `libmysql.dll` from root into `GameServer\` fixed it. After these, GameServer reached `GameServer Ready` + all 3 internal handshakes (SERVERLINK 55555, AUTHSERVER 44417, CONNECTSERVER 44419) succeeded. Remaining open issue: GameServer crashes at `Loading Castle Siege NPC From ServerLink` — likely a struct-layout/packet mismatch because the 4 binaries were built at different times (GameServer 14:21 vs others 13:41); fix = rebuild all 4 from one consistent build. **S16 stack: MySQL 5.6 Docker (port 3307) with 4 DBs (`database_login`=16 tbl, `database_game`=179, `database_characters`=97, `database_log`=21); runtime binaries + conf in `D:\Project\mutopia\server-s16\`; start order = LoginServer → ConnectServer → ServerLink → GameServer.**
- **Season 16 Multi-Realm Architecture & Client Strategy Assessment (2026-08-19) — DECISION**:
  - **Server S16 Native (`src/server-s16` - C++ MSVC Win32)**: Verified 100% compiled clean with 0 errors (`GameServer.exe`, `ConnectServer.exe`, `DataServer.exe`), schema validated on SQL Server 2022 (63 tables + Castle Siege, Character, Item data). Full Season 16 business logic (10 classes, Gun Crusher/Slayer/Rune Wizard, 4th class skill tree, Ruud shop, Socket Earring, Pentagram).
  - **Client S16 Root Cause & Strategy Comparison**:
    1. `client-s16` (Lgd-Client): Legacy 2021 `main.exe` packed with SCY/Themida; reverse-engineered and patched 8+ crash traps (GetCommandLineA, Null deref, SSE Float, SSO string, UTF-8 converter, texture streamer callback, window event check).
    2. `MuEmu-master` (C# .NET Core Multi-Season Emulator): Verified compiles 100% clean (0 errors). Contains full Season 16 DTO packet serializations (`MU.Network`) and comprehensive client patch definition `ClientConfig/Config_1.19.46.0.ini` (17 crash traps, GameGuard bypass, Floating point, ResourceGuard, Gun Crusher creation).
    3. `MU VoZ SS16.1` (Production Client): Verified complete standalone Season 16.1 production client. Features dynamic INI configuration (`config.ini` with `IpAddress`, `Port`, `Version = 1.07.17`, `Serial = 0hfqckZw30CKOzZA`, `ANTILAG`, `FONT`), pre-matched `Main.dll` (1.09MB) + `CCD.dll` + `DSETUP.dll`, without restrictive proprietary wrapper (`game.bin`/`auth.bin` seen in `ZhyperMU S16 Full`). Designated as the recommended client runtime for the Modern S16 realm.
  - **Evolution Strategy**: Two parallel viable paths: (A) Run native S16 using `MU VoZ SS16.1` client linked to `src/server-s16` C++ server, (B) Port S16 calculation formulas and event state machines from C++ into `MuEmu-master` C# for a modern, cross-platform, multi-season server.










---archived from .copilot/memory/MEMORY.md on 2026-08-27---
